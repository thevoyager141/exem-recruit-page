# C 에이전트 개발자가 말하는 "죽지 않는 에이전트"의 비밀

## 인트로

EXEM의 모니터링 솔루션이 고객 환경에서 안정적으로 동작하려면, 그 시작점에서 원천 데이터를 수집하는 에이전트가 반드시 필요합니다. 인프라부터 네트워크, APM(Application Performance Management)까지 — 이 에이전트를 만드는 개발자를 만났습니다.

통합개발본부 데몬3팀 차장은 EXEM에서 5년간 C 언어 기반 데이터 수집 에이전트를 개발해 왔습니다. exemONE과 InterMax에서 고객이 장애 원인을 빠르게 분석할 수 있도록, 모니터링의 기반이 되는 원천 데이터 수집 로직을 설계하고 구현하는 역할을 맡고 있습니다.

Linux, AIX, HP-UX, Windows 등 다양한 OS 환경에서 동작하는 에이전트를 만들고, SNMP 기반 네트워크 장비 모니터링과 TP 미들웨어(Middleware) 트랜잭션(Transaction) 분석까지 — 폭넓은 기술 영역을 직접 다루고 있는 개발자의 이야기를 들어봤습니다.

---

## 커리어 — 에이전트 하나로 다섯 가지 OS를 넘나들다

**Q. EXEM에서 개발자로서 시야가 넓어졌다고 느끼신 부분이 있나요?**

입사 전에는 리눅스(Linux) 환경에서 C 언어 에이전트 개발만 했는데, EXEM에 오고 나서는 C 언어를 제외하면 기존 경험과 겹치는 영역이 거의 없을 정도로 새로운 기술을 접하게 됐어요.

입사 초기에는 TMAX, TUXEDO 기반 TP 미들웨어 구조와 트랜잭션 흐름을 이해해야 했습니다. 생소한 영역이라 시간이 걸렸지만, 다른 곳에서는 접할 수 없는 기술 경험이었어요. 인프라 영역에서는 AIX, HP-UX, SunOS, Windows 등 다양한 OS를 직접 다뤘는데, 같은 C 언어라도 OS마다 커널(Kernel) 인터페이스, 시스템 API, 성능 지표 수집 방식이 완전히 달라서 각 운영체제의 내부 구조를 깊이 이해하는 계기가 됐습니다. 네트워크 영역에서는 SNMP 기반 장비 모니터링을 하면서 MIB 구조, OID 체계 같은 네트워크 장비 특성도 배울 수 있었고요.

최근에는 파이썬(Python) 모니터링 에이전트 개발을 통해 컨테이너(Container) 및 쿠버네티스(Kubernetes) 환경까지 접하게 됐습니다. 담당하는 에이전트들은 모두 모니터링이라는 공통 목적을 가지지만 각기 다른 기술 영역을 다루기 때문에, 5년간 정말 많이 성장할 수 있었습니다.

---

## 업무 — 고객 서버 위에서 24시간 돌아가는 코드를 만든다는 것

**Q. 24시간 운영 환경에서 동작하는 C 기반 에이전트를 개발할 때 가장 중요한 원칙은 무엇인가요?**

가장 중요한 원칙은 "죽지 않는 에이전트"를 만드는 것입니다. 고객 서버에서 직접 실행되기 때문에 패치가 쉽지 않고, 일부 환경에서는 에이전트 장애가 곧 서비스 영향으로 이어질 수 있거든요. 그래서 개발 단계부터 메모리 사용량, CPU 부하, 리소스(Resource) 누수 가능성을 꼭 점검하고, 단기 테스트가 아니라 장시간 실행 테스트를 통해 안정성을 검증합니다.

또 하나 중요한 부분은 하위 호환성 유지예요. 최신 에이전트와 구버전 수집 서버가 함께 운영되더라도 문제없이 동작해야 하기 때문에, 기능 확장도 중요하지만 항상 호환성과 안정성을 우선으로 생각합니다.

**Q. 고객 환경마다 에이전트가 다르게 동작했던 경험이 있나요?**

호스트(Host) 에이전트는 리눅스만 해도 배포판과 버전이 매우 다양한데, 특정 버전에서 일부 성능 지표가 수집되지 않거나, 예상보다 많은 데이터가 수집되어 내부 버퍼(Buffer) 구조를 초과하는 문제가 발생한 적이 있었습니다. 이 경험을 통해 특정 OS 버전에 맞춘 예외 처리 방식이 아니라, 통합적으로 대응할 수 있는 구조로 로직을 개선하는 방향으로 접근하게 됐어요.

---

## 문화 — "내 코드만 문제없으면 된다"가 아닌 팀

**Q. 데몬그룹의 개발 문화 중 특히 좋다고 느끼신 부분은 무엇인가요?**

가장 좋은 점은 개인 역할에만 한정되지 않고, 솔루션 전체 품질을 함께 생각하는 문화입니다. 에이전트는 기능 단위로 보면 데이터 수집까지가 역할이지만, 팀에서는 수집 서버에서 API, 화면까지 이어지는 전체 흐름을 검증하면서 데이터 정합성을 점검해요. 문제가 있으면 담당자에게 바로 공유해서 함께 개선합니다. 최종 사용자에게 전달되는 결과까지 함께 고려하는 분위기 덕분에 제품 완성도가 높아지고, 시스템 전체 구조를 이해할 수 있게 됐습니다.

기술 공유와 아키텍처(Architecture) 토론 문화도 좋아요. 설계 단계에서부터 서로 의견을 주고받고, 막히는 부분이 있을 때 부담 없이 논의할 수 있는 분위기라서 다양한 시각의 장점을 모아 더 완성도 높은 구조를 만들 수 있습니다. 장애 대응 시에도 로그, 수집 데이터, 재현 조건을 함께 검토하면서 서로 다른 관점에서 코드 리뷰를 진행하고, 최근에는 Claude Code 같은 AI 도구도 활용해 사람이 놓칠 수 있는 부분을 추가로 점검하고 있습니다.

---

## 목표 — 함께 성장하는 환경을 만드는 개발자가 되고 싶다

**Q. 앞으로 EXEM에서 도전해보고 싶은 목표가 있나요?**

쿠버네티스 환경에 대한 이해도를 높여서 더 적극적으로 활용해 보고 싶습니다. 그리고 AI 기술도 코드 분석, 테스트 자동화, 장애 원인 추적, 성능 검증 같은 영역에서 적극 활용해 에이전트 품질과 개발 생산성을 동시에 높이는 환경을 만들어보고 싶어요.

개발자로 일한 지 12년이 됐는데, 단순히 경험을 전달하는 선배가 아니라 함께 성장하는 환경을 만드는 역할을 하고 싶습니다. 후배들의 새로운 시각에서 저도 배우고, 제 경험으로 팀원들에게 도움이 되는 사람이 되고 싶어요.

**Q. EXEM 합류를 고민하는 분들에게 한마디 부탁드려요.**

C 기반 개발은 분명 어렵지만, 그만큼 시스템을 깊이 이해하는 개발자가 될 수 있습니다. 메모리, 성능, 구조를 직접 설계하고 최적화하는 경험은 어떤 언어를 사용하더라도 큰 기반이 돼요. EXEM의 에이전트 개발은 단순한 애플리케이션 개발이 아니라, 실제 고객 서버에서 동작하는 시스템 에이전트를 만드는 일입니다. 동시에 쿠버네티스, 네트워크, APM 등 다양한 영역을 계속 접할 수 있어서 기술적으로 폭넓게 성장할 수 있는 환경이라고 생각합니다.
